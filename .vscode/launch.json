{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Nexora Host (Development)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: Build (Windows Development)",
      "stopAtEntry": false,
      "console": "integratedTerminal"
    }
  ]
}
