include(NexoraTarget)
add_library(NexoraRuntime ${NEXORA_MODULE_LIBRARY_TYPE}
  src/InputUi.cpp
  src/GameplayModuleHost.cpp
  src/GameWorld.cpp
  src/GameplayHostBridge.cpp
  src/Runtime.cpp)
if(NEXORA_ENABLE_GAMEPLAY_SIMULATION)
  target_sources(NexoraRuntime PRIVATE src/GameplaySimulation.cpp)
endif()
if(NEXORA_ENABLE_ASSET_PIPELINE)
  target_sources(NexoraRuntime PRIVATE src/AssetPipeline.cpp src/BundleMount.cpp)
endif()
if(NEXORA_ENABLE_EDITOR_SDK)
  target_sources(NexoraRuntime PRIVATE src/EditorSdk.cpp)
endif()
if(NEXORA_ENABLE_PRESENTATION)
  target_sources(NexoraRuntime PRIVATE src/Presentation.cpp)
endif()
if(NEXORA_ENABLE_LARGE_WORLD)
  target_sources(NexoraRuntime PRIVATE src/LargeWorld.cpp)
endif()
if(NEXORA_ENABLE_PLATFORM)
  target_sources(NexoraRuntime PRIVATE src/Platform.cpp)
endif()
if(NEXORA_ENABLE_SHIPPING)
  target_sources(NexoraRuntime PRIVATE src/Shipping.cpp)
endif()
add_library(Nexora::Runtime ALIAS NexoraRuntime)
target_include_directories(NexoraRuntime PUBLIC "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>")
target_link_libraries(NexoraRuntime PUBLIC Nexora::Renderer)
if(NOT WIN32)
  target_link_libraries(NexoraRuntime PRIVATE ${CMAKE_DL_LIBS})
endif()
target_compile_definitions(NexoraRuntime PRIVATE NEXORA_RUNTIME_EXPORTS)
if(NEXORA_ENABLE_ASSET_PIPELINE)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_ASSET_PIPELINE_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_ASSET_PIPELINE_ENABLED=0)
endif()
if(NEXORA_ENABLE_EDITOR_SDK)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_EDITOR_SDK_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_EDITOR_SDK_ENABLED=0)
endif()
if(NEXORA_ENABLE_SCENE_RENDERING)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_SCENE_RENDERING_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_SCENE_RENDERING_ENABLED=0)
endif()
if(NEXORA_ENABLE_GAMEPLAY_SIMULATION)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_GAMEPLAY_SIMULATION_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_GAMEPLAY_SIMULATION_ENABLED=0)
endif()
if(NEXORA_ENABLE_PRESENTATION)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_PRESENTATION_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_PRESENTATION_ENABLED=0)
endif()
if(NEXORA_ENABLE_LARGE_WORLD)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_LARGE_WORLD_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_LARGE_WORLD_ENABLED=0)
endif()
if(NEXORA_ENABLE_PLATFORM)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_PLATFORM_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_PLATFORM_ENABLED=0)
endif()
if(NEXORA_ENABLE_SHIPPING)
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_SHIPPING_ENABLED=1)
else()
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_SHIPPING_ENABLED=0)
endif()
if(NEXORA_LINK_MODE STREQUAL "Monolithic")
  target_compile_definitions(NexoraRuntime PUBLIC NEXORA_RUNTIME_STATIC=1)
endif()
nexora_apply_defaults(NexoraRuntime)
